# AGENTS.md

Entry point for AI coding agents working in this repo (`ctxr-dev/.github`, locally cloned as `start/`). This repo backs the **Contexter** org landing at <https://github.com/ctxr-dev>.

## Skills

- [`.agents/skills/keep-org-profile-fresh`](.agents/skills/keep-org-profile-fresh/SKILL.md) — how to maintain this repo: where files live, the committed design language (dark-futurist, violet/cyan/amber on midnight), brand rules (Contexter as the human name; `ctxr-dev` only as a URL slug; no 🆕 emoji; no `(default)` inline), how to update the animated hero SVG and the typing terminal loop, how to add a new package row, and the validation checks to run before pushing. **Read this before any edit to `profile/README.md` or `profile/assets/*.svg`.**

## Quick orientation

- `profile/README.md` is the org landing GitHub renders at <https://github.com/ctxr-dev>.
- `profile/assets/hero.svg` is the animated header (12-node constellation, typing terminal, sparkles, HUD).
- `profile/assets/footer.svg` is the slim signal trail under the contribute block.
- `README.md` at the repo root is a 5-line pointer; do not duplicate the landing content there.
- Remote: `origin` is `https://github.com/ctxr-dev/.github.git`. The local folder is named `start/` for legacy reasons; keep it.
